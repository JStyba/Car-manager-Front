'use strict';

describe('myApp.addRepair module', function() {

  beforeEach(module('myApp.addRepair'));

  describe('addRepair controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addRepairCtrl = $controller('addRepairCtrl');
      expect(addRepairCtrl).toBeDefined();
    }));

  });
});